spring:
  datasource:
    driver-class: name=com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/board_study?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    password: chocolate52
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true